<UserControl x:Class="TorchPlugin.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TorchPlugin"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" SharedSizeGroup="Labels" />
            <ColumnDefinition Width="Auto" SharedSizeGroup="Inputs" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" SharedSizeGroup="Buttons" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Text="Se_web" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="5" />

        <!-- Enabled -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Enabled" VerticalAlignment="Center" Margin="5" />
        <CheckBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Name="Enabled" IsChecked="{Binding Enabled}" Margin="5" />

        <!-- DetectCodeChanges -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Detect code changes" VerticalAlignment="Center" Margin="5" />
        <CheckBox Grid.Row="2" Grid.Column="1" Name="DetectCodeChanges" IsChecked="{Binding DetectCodeChanges}" Margin="5" />
        <TextBlock Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Text="Enables checking for changes in patched game code (disable this on Proton/Linux)" VerticalAlignment="Center" Margin="5" />

        <!-- Server ID -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Server ID" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Name="ServerId" Text="{Binding ServerId}" Margin="5" />

        <!-- Database Server -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Database Server" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Name="DatabaseServer" Text="{Binding DatabaseServer}" Margin="5" />

        <!-- Database Name -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Database Name" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Name="DatabaseName" Text="{Binding DatabaseName}" Margin="5" />

        <!-- Database User -->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Database User" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Name="DatabaseUser" Text="{Binding DatabaseUser}" Margin="5" />

        <!-- Database Password -->
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Database Password" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Name="DatabasePassword" Text="{Binding DatabasePassword}" Margin="5" />

        <!-- Damage Log API URL -->
        <TextBlock Grid.Row="8" Grid.Column="0" Text="Damage Log API URL" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2" Name="DamageLogApiUrl" Text="{Binding DamageLogApiUrl}" Margin="5" />

        <!-- API Base URL -->
        <TextBlock Grid.Row="9" Grid.Column="0" Text="API Base URL" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="2" Name="ApiBaseUrl" Text="{Binding ApiBaseUrl}" Margin="5" />

        <!-- Save Button -->
        <Button Grid.Row="10" Grid.Column="2" Content="Save" Margin="5" Click="SaveButton_OnClick" />
    </Grid>
</UserControl>
